# 网站设计风格复制 Skill 可行性分析

## 1. 结论

这个想法可以实现，但应把目标准确地定义为：

> 从目标网站的可见页面、DOM、计算后样式、响应式变化和交互状态中提取设计证据，将其归纳成一套可执行的设计规范，并以幂等方式写入项目根目录的 `AGENTS.md`、`CLAUDE.md` 或 `DESIGN.md`。

它适合做“设计系统逆向工程”和“风格迁移”，不适合承诺在没有完整页面、状态和素材的情况下，自动得到 100% 像素级、行为级的复制品。

在具备浏览器自动化、页面可正常访问、代表性页面足够、主要交互状态可触发的条件下，下列内容有较高的提取可靠性：

- 主题色、背景色、文字色、边框色和状态色
- 字体族、字号、字重、行高、字距和标题层级
- 页面最大宽度、栅格、栏数、容器、间距和留白节奏
- 圆角、边框、阴影、模糊、透明度和层级
- 按钮、输入框、卡片、导航、表格、标签、弹窗等常见组件
- 桌面端、平板端、移动端的布局变化
- hover、focus、active、disabled、selected 等状态
- 动画时长、缓动函数和常见过渡方式

较难自动、完整提取的内容包括登录后页面、实验分流页面、复杂 Canvas/WebGL、跨域 iframe、关闭的 Shadow DOM、隐藏流程、服务端按用户下发的差异，以及需要真实业务数据才能出现的异常状态。

因此，推荐把 skill 定位为“高保真设计规范生成器”，而不是“网页源代码复制器”。

## 2. “复制设计风格”的合理边界

### 2.1 应复制的内容

skill 应提取设计语言和可复用规则：

- 设计令牌：颜色、字体、间距、圆角、阴影、描边、尺寸、层级、动效
- 布局规则：容器、栅格、对齐、密度、响应式断点和页面骨架
- 组件规则：结构、尺寸、变体、状态和组合关系
- 视觉原则：对比度、留白、信息层级、强调方式和内容密度
- 实施约束：在新项目中应该怎样使用这些规则，以及应该避免什么

### 2.2 不应默认复制的内容

skill 不应默认搬运：

- 目标网站的 HTML、JavaScript、业务代码或整份 CSS
- 文案、数据库内容、用户数据和内部接口
- Logo、商标、专有插画、摄影作品和付费字体文件
- 需要授权的图标集或受许可限制的资源
- 用于冒充原网站、误导用户或绕过安全机制的细节

输出中可以记录目标站使用了哪一类视觉资产，例如“线性图标、1.5px 描边、圆角端点”，但应优先给出可替代的实现规则，而不是直接下载资产。

这不是法律意见。对外发布高度相似的商业界面前，仍需要结合使用地区、素材许可、商标和整体商业外观风险进行审查。

## 3. 为什么技术上可行

浏览器最终必须把设计转换成可观测结果。只要页面能被正常渲染，就能从多个证据层提取信息：

| 证据层 | 可获得信息 | 可靠性 |
| --- | --- | --- |
| 截图 | 最终颜色、视觉层级、留白、构图、图片和整体感受 | 中；看得见但难区分实现原因 |
| DOM 与语义 | 元素层级、组件边界、表单语义、重复结构 | 高；但不代表最终视觉 |
| `getComputedStyle` | 实际生效的字体、颜色、边距、圆角、阴影和布局 | 高 |
| CSS 变量和样式表 | 原始 token、断点、状态选择器、动效定义 | 高；可能受跨域样式表限制 |
| 元素几何信息 | 尺寸、间隔、对齐、容器、栅格和密度 | 高 |
| 多视口对比 | 响应式断点和重排策略 | 高 |
| 交互录制 | hover、focus、弹层、菜单和状态切换 | 中到高；取决于覆盖范围 |

只依赖截图会丢失字体来源、精确间距、响应式和隐藏状态；只依赖 DOM/CSS 又容易收集大量无关声明。因此必须采用“视觉证据 + 运行时样式 + 几何统计 + 语义归纳”的组合方法。

## 4. 主要难点

### 4.1 不是每个 CSS 值都是设计规则

一个真实网站可能有数万个 CSS 声明，其中包含浏览器默认值、一次性修正值、第三方组件样式和隐藏节点。直接把这些值写入文档会产生噪声。

解决方式是做频次、面积、语义和跨页面一致性分析：

- 优先保留重复出现的值
- 提高可见区域和大面积元素的权重
- 区分正文、标题、边框、背景、交互状态等角色
- 将相近值聚类，但保留原始证据
- 对只出现一次的值标为局部例外，而不是全局 token

### 4.2 “视觉角色”需要推断

`#2563eb` 只是一个颜色值，“主操作色”才是可执行规则。角色推断需要结合元素语义、位置、点击属性、对比度和出现频率，并附带置信度，避免把链接色误判成品牌主色。

### 4.3 页面覆盖范围决定结果上限

只分析首页，通常看不到表单错误、空状态、数据表格、弹窗、复杂导航和移动端菜单。skill 应自动发现同域代表页面，但必须设置数量上限，并允许用户提供关键路由和状态清单。

### 4.4 响应式不是缩放

移动端可能改变导航结构、内容顺序、栅格、组件尺寸和交互模型。至少要在桌面、平板、手机三个视口采样，并比较元素的出现、消失、移动和尺寸变化。

### 4.5 字体和素材可能不可复用

可以检测实际使用的字体名称、fallback、字重和渲染尺寸，但不应自动复制受限制的字体文件。输出应同时记录原字体和建议的合法替代字体。

### 4.6 交互采集可能产生副作用

自动点击可能提交表单、发送消息、删除数据或产生订单。默认只允许无副作用动作，例如 hover、focus、展开菜单、打开并关闭弹窗；任何提交、购买、删除或外部消息动作都必须排除。

## 5. 建议的能力分级

### 5.1 增强模式

适用于能使用 Chromium DevTools Protocol、Playwright 或等价浏览器自动化的环境。

能力包括：

- 采集截图、DOM、计算样式、元素几何和 CSS 变量
- 扫描同域代表页面
- 切换多个视口
- 触发安全的交互状态
- 读取开放 Shadow DOM 和同源 iframe
- 生成高置信度 token 和组件规则

这是推荐的默认模式。

### 5.2 视觉模式

适用于只能访问截图，或目标站阻止自动化/DOM 读取的情况。

仍可分析：

- 色彩、字体外观、布局比例、圆角、阴影、留白和视觉密度
- 可见组件及其大致状态

但必须把精确数值标为估算值，并明确提示未覆盖响应式、隐藏状态和真实 CSS token。

### 5.3 人工补充模式

用户可提供：

- 关键路由
- 登录后的截图
- 展开菜单、弹窗、错误状态等状态截图
- 品牌字体和素材许可信息
- 希望优先模仿或刻意排除的部分

skill 将这些输入与自动证据合并。对无法安全自动触发的页面，这是提高质量的必要途径。

## 6. 推荐的输出策略

项目根目录按以下规则选择目标文件：

| 根目录状态 | 写入行为 |
| --- | --- |
| 仅存在 `AGENTS.md` | 更新其中的受管设计区块 |
| 仅存在 `CLAUDE.md` | 更新其中的受管设计区块 |
| 两者都存在 | 向两者写入内容一致的受管设计区块 |
| 两者都不存在，已有 `DESIGN.md` | 更新其中的受管设计区块 |
| 三者都不存在 | 创建 `DESIGN.md` |

不应使用无条件文本追加。每个来源应有稳定的受管标记，重复运行时替换对应区块：

```md
<!-- copy-design:start id=8f36c1e94a2b -->
...生成的设计规范...
<!-- copy-design:end id=8f36c1e94a2b -->
```

`id` 由去除敏感查询参数后的规范化来源和 profile 名称生成。这样可以：

- 保留文件中所有人工内容
- 避免重复运行产生多份相同规范
- 支持一个项目保存多个来源风格
- 精确校验只修改了受管区块

当 `AGENTS.md` 和 `CLAUDE.md` 都存在时写入相同区块，是为了让不同编码代理获得一致约束；后续更新必须同时成功，否则应回滚本次修改或报告部分失败。

## 7. 输出内容应达到的粒度

最终文档不应是 CSS 属性清单，而应是编码代理能直接执行的规则，至少包含：

1. 来源、采样页面、视口、采集时间和覆盖范围
2. 设计风格摘要
3. 色彩、字体、间距、圆角、边框、阴影和层级 token
4. 页面容器、栅格、主要区域和留白规则
5. 常见组件的结构、变体和状态
6. 响应式规则
7. 交互和动效规则
8. 可访问性约束
9. 推荐做法和禁止做法
10. 推断项、缺失状态和置信度说明

原始 DOM、整份 CSS 和逐元素样式不应进入 `AGENTS.md` 或 `CLAUDE.md`，否则会造成上下文膨胀并降低后续代理执行质量。

## 8. 风险与缓解方案

| 风险 | 影响 | 缓解方案 |
| --- | --- | --- |
| 页面无法访问或需要登录 | 证据不足 | 使用用户已登录的浏览器会话或用户提供的截图，不收集凭据 |
| 反爬、验证码、地区限制 | 自动采集失败 | 停止绕过，切换视觉/人工补充模式 |
| 页面过多 | 时间和 token 成本过高 | 同域、代表性优先、默认限制页面数和状态数 |
| 动态内容导致结果不稳定 | 重复采集差异大 | 等待字体和布局稳定，关闭动画，记录时间和页面状态 |
| A/B 测试 | 得到局部版本 | 记录实验痕迹和采样时间，多次采样后标记差异 |
| 错误的语义推断 | 规范误导实现 | 保存证据、置信度和例外，低置信度项不升级为强制规则 |
| 文档越写越长 | 代理上下文受污染 | 受管区块、去重、聚类、长度预算和摘要优先 |
| 覆盖用户已有规范 | 破坏项目约束 | 只修改受管区块；既有明确项目规范优先，并报告冲突 |
| 敏感信息进入文档 | 泄露 token、用户数据 | 清理 URL 查询参数、Cookie、localStorage、表单值和截图中的私密数据 |
| 知识产权或品牌混淆 | 发布风险 | 不复制源码和专有资产，输出替代建议，提醒发布前审查 |

## 9. 实施范围建议

### MVP

- 接受一个公开 URL
- 支持桌面、平板、手机三种视口
- 分析一个主页面和有限数量的同域代表页面
- 提取核心 token、布局和常见组件
- 生成带来源和置信度的 Markdown
- 按目标文件矩阵幂等写入
- 校验没有修改受管区块之外的内容

### 第二阶段

- 自动发现组件状态
- CSS 变量和媒体查询深度解析
- 页面类型聚类
- 开放 Shadow DOM、同源 iframe
- 截图与规范的视觉回归验证

### 第三阶段

- 根据规范直接改造或生成前端页面
- 将实现结果与目标截图做视觉差异比较
- 形成“提取 → 实现 → 对比 → 修正”的闭环

第三阶段应作为另一个明确动作，不能默认由“分析并保存设计规范”隐式触发。

## 10. 成功标准

这个 skill 可以在满足以下条件时视为成功：

- 重复运行不会产生重复区块
- 除受管区块外不改动用户已有文件内容
- 每个关键规则都能追溯到直接证据或明确标为推断
- 输出同时覆盖桌面端和移动端差异
- 输出是语义化设计规则，而不是原始 CSS 值堆积
- 后续编码代理只阅读输出文档，就能构建出明显属于同一设计语言的新页面
- 无法访问、证据不足或置信度低时会降级并说明，不伪造精确结论
- 不默认下载受限制的源码、字体、图片或品牌资产

## 11. 最终判断

该想法具有明确价值，也适合做成通用 Agent Skill。核心工作流应由 `SKILL.md`、可执行脚本和 references 构成；厂商专用元数据只能作为可选适配层。真正决定质量的不是“能否截图”，而是四件事：

1. 是否采集了足够多的页面、视口和状态
2. 是否能把大量运行时样式压缩成语义化规则
3. 是否保留证据和不确定性，而不是过度推断
4. 是否安全、幂等地维护项目中的设计规范

本仓库已经按 `DESIGN.md` 的核心架构实现首个可运行 MVP，并通过本地响应式测试站的真实浏览器采集。后续可继续增加自动页面发现、更复杂的安全交互状态和实现页面视觉回归能力。
